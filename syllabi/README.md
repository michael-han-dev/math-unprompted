# Syllabi

Drop course syllabi here (PDF, .txt, .md, .docx). Everything in this folder except this
README is gitignored, so the files stay on your machine.

They are used to build the topic lists in `src/data/courses.ts`: definitions and objects
become **concepts** (off-the-cuff pool), named results become **theorems** (deep-research pool).
